services:
  - type: web
    name: chem
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "rm -f telegram_mini_app.db && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DEV_MODE
        value: False
